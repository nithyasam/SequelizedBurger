<div class ="container">
<div class="row">
<div class="col-lg-4" id="not_devoured">
  <h2>Burgers</h2>

  {{#each burgers}}
    {{#unless this.devoured}}
      <div class="row">
      <div class="col-lg-8">
        <span class="label label-default burger_label">{{this.id}}. {{this.burger_name}}</span>
      </div>
      <div class="col-lg-4">
        <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="true">
          <button type="submit">devoure it</button>
        </form>
        </div>
       </div>
    <br>
    {{/unless}}
  {{/each}}

</div>
<div class="col-lg-4"></div>
<div class="col-lg-4" id="devoured">
  <h2>Devoured Burgers</h2>

  {{#each burgers}}
    {{#if this.devoured}}
        <br>
        <span class="label label-success burger_label">{{this.id}}. {{this.burger_name}}</span>
        <br>
    {{/if}}
  {{/each}}

<br>
</div>
</div>
<br><br>
<div class="row">
<div class="col-lg-4"></div>
<div class="col-lg-4" id="newburger">

<form class="create-update-form" action="/" method="POST">
  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>
  <button type="submit" class="btn btn-default">Add Burger</button>
</form>

</div>
<div class="col-lg-4"></div>
</div>
</div>